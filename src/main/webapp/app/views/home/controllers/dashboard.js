define(['app'], function(app) {
	app.controller('HomeController', function Controller($window, Table) {
		var that = this;
		
		that.headers = [ 'A', 'B', 'C' ];
		
		that.init = function() {
			// init
		};
	});
});